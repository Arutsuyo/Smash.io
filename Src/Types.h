#pragma once

#ifndef _TYPES_
#define _TYPES_

#define _NUM_BUTTONS 5

enum VsType
{
    Self = 0,
    CPU = 1,
    Human = 2
};

enum Button
{
    A = 0,
    B = 1,
    X = 2,
    Z = 3,
    L = 4,
    None
};

#endif